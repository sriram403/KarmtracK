app = "notekeeper-app"
primary_region = "sin"

[build]

[env]
  DB_PATH = "/data/pkm.db"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[[mounts]]
  source = "notekeeper_data"
  destination = "/data"
